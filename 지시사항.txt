You are modifying research_loop.py.

Your goal is to add a deterministic post-check after Claude final decision so that Path B / Path C cannot be selected unless the code-level guardrails are actually satisfied.

Do NOT redesign the current architecture.
Preserve:
- GPT result interpretation
- Gemini short diagnosis
- consensus building
- GPT patch proposals for Path A only
- Claude as the final decision maker
- current Path A / B / C / done semantics
- current revision_request flow
- current METRICS:{...valid json...} contract

==================================================
TASK — Add a code-level post-check after Claude final decision
==================================================

Current problem:
- research_loop.py already has strong logic in _build_consensus()
- Claude final decision prompt also describes strict conditions for Path B and Path C
- however, after Claude returns a final decision, there is no deterministic code-level recheck that enforces those same rules
- this means Claude could still return B or C even when the formal guardrails are not actually satisfied

Required change:
Add a post-check stage immediately after _claude_final_decision(...) returns.

Implement a helper such as:
- _postcheck_final_decision(...)
or
- _validate_claude_decision(...)
that receives at minimum:
- claude decision
- consensus summary
- gpt interpretation
- gemini diagnosis
- run history
- latest result summary

This helper must return:
- corrected/final decision JSON
- optional adjustment reason
- whether the original Claude decision was overridden

==================================================
POST-CHECK RULES
==================================================

1. done
- allow if the existing done criteria are satisfied
- otherwise downgrade to consensus_path_candidate or A as appropriate

2. Path A
- generally allowed as the lowest-risk revision path
- preserve current behavior unless there is a very strong reason not to

3. Path B
Allow Path B only if all required conditions are satisfied:
- GPT suggested_path is B or C
- Gemini is not "disagree"
- consensus agreement_level is at least "medium"
- there are at least 2 runs of evidence
- the latest failure is not just a pure smoke/runtime/execution failure
- the case really fits “hypothesis refinement without replacing the core idea”

If these conditions are not satisfied:
- downgrade B to A
- or downgrade to consensus_path_candidate if that is safer
- add an explicit adjustment note

4. Path C
Allow Path C only if all required conditions are satisfied:
- repeated evidence exists
- consensus agreement_level is "strong"
- GPT evidence_strength is "high"
- enough runs exist (preserve current strong threshold logic)
- this is not just an implementation/training instability case
- the case really fits “core mechanism or claimed advantage is unsupported”

If these conditions are not satisfied:
- downgrade C to B if B is allowed
- otherwise downgrade C to A
- add an explicit adjustment note

==================================================
IMPLEMENTATION DETAILS
==================================================

Implement the post-check as code logic, not as another LLM call.

Preserve Claude as the final decision maker in spirit, but make the system safe by enforcing hard policy rules after Claude returns.

The post-check output should remain machine-readable.
Recommended fields to add/update in the returned decision:
- path
- decision_reason
- consensus_level
- postcheck_applied: bool
- postcheck_override: bool
- postcheck_note: str | None

If Claude’s original path is changed by the post-check:
- record both the original path and the corrected path if practical
- log the reason clearly
- keep the system auditable

==================================================
WHERE TO INTEGRATE
==================================================

Integration point:
- run GPT interpretation
- run Gemini diagnosis
- build consensus
- if candidate path == A, generate GPT patches
- run Claude final decision
- THEN run the new post-check
- use the post-checked decision as the actual decision for downstream behavior

Downstream behavior must use the corrected decision, not the raw Claude output.

This includes:
- Path A package regeneration
- Path B / C revision_request creation
- done termination
- run_history recording

==================================================
RUN HISTORY / TRACEABILITY
==================================================

When the post-check changes Claude’s decision:
- store the raw Claude path
- store the final corrected path
- store whether post-check override happened
- store a short post-check reason in run history / previous_results.jsonl when feasible

This is important so later analysis can see:
- what Claude wanted
- what the system allowed
- why the correction happened

==================================================
CONSTRAINTS
==================================================

- Use small helper functions and local refactors
- Do not rewrite the whole loop
- Preserve current structured JSON outputs
- Preserve current package generation flow
- Preserve current revision_request flow
- Preserve current Path A/B/C meanings
- Keep everything deterministic and machine-readable

==================================================
DELIVERABLES
==================================================

After modifying the code, provide a short summary:
1. what new post-check helper was added
2. what exact rules are enforced for B and C
3. how overrides are recorded
4. where the corrected decision is now used
5. any remaining TODOs