You are modifying research_loop.py.

Your goal is to tighten the remaining decision logic without redesigning the overall architecture.

Do NOT rewrite unrelated parts.
Preserve:
- the current GPT interpretation stage
- the current Gemini short diagnosis stage
- the current consensus layer
- the current Claude final decision stage
- the current Path A / B / C / done semantics
- the current package generation flow
- the current METRICS:{...valid json...} contract
- the current revision_request flow for Path B / C

Apply the following three focused improvements.

==================================================
TASK 1 — Make Path B decision criteria stricter and more explicit
==================================================

Current problem:
- Path C already has strong guardrails
- but Path B is still relatively loose and depends too much on Claude discretion
- this makes the A vs B boundary less stable than it should be

Required change:
Strengthen the rule for when Path B is allowed.

Implement explicit Path B gating rules such as:
- Path B is allowed only if GPT suggested_path is B or C
- Gemini must be at least agree or partial, not strong disagreement
- consensus agreement_level must be at least medium
- Path B must not be selected for pure smoke/runtime/execution failures
- Path B should require at least 2 runs of evidence unless there is unusually strong justification
- Path B should mean:
  the core idea is still directionally valid, but the claim/scope/condition needs refinement

Also make Claude final decision prompt reflect this clearly:
- A = implementation/training/experimental-design issue
- B = hypothesis refinement without replacing the core idea
- C = core mechanism or claimed advantage is no longer supported

Do not make Path B as expensive as Path C, but make it significantly stricter than it is now.

==================================================
TASK 2 — Generate GPT patches only when consensus candidate is Path A
==================================================

Current problem:
- patch proposals are currently generated too early or too broadly
- if the likely path is B, generating code patches mixes implementation improvement with hypothesis refinement

Required change:
Restrict patch generation to Path A only.

Implement:
- call _gpt_propose_improvements() only when consensus_path_candidate == "A"
- do not generate code patches for B, C, or done
- for B, allow only refinement notes / hypothesis-plan adjustment hints, not code patches
- preserve the current structured patch proposal format for A

Important:
- keep Path A practical and implementation-focused
- keep Path B as hypothesis refinement, not code patch mode
- keep Path C as hypothesis revision/replacement, not code patch mode

==================================================
TASK 3 — Enrich run_history so future decisions have better evidence
==================================================

Current problem:
- run_history is useful but still too thin
- later decisions would be stronger if the loop remembered not only metrics/status, but also decision and consensus context

Required change:
Extend what is stored in run_history / previous_results.jsonl.

At minimum, add fields such as:
- decision_path
- consensus_level
- gpt_suggested_path
- gemini_suggested_path
- accepted_patch_indexes
- training_stability summary
- blocked_reason if package generation was blocked
- validation_failed if relevant

Requirements:
- preserve backward compatibility where possible
- do not break existing downstream usage
- keep the history machine-readable
- keep the added fields concise and stable
- use these richer history records in later interpretation/decision prompts when helpful

==================================================
IMPLEMENTATION CONSTRAINTS
==================================================

- Use helper functions and small local refactors
- Do not redesign the whole system
- Preserve current output schemas where possible, only extend them
- Keep everything machine-readable
- Keep the code deterministic and automation-friendly

==================================================
DELIVERABLES
==================================================

After modifying the code, provide a short summary:
1. how Path B became stricter
2. how patch generation is now restricted to Path A only
3. what new fields are now stored in run_history
4. any remaining TODOs